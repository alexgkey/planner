{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.address) }}
    {{ form_row(form.color) }}

    <div class="d-flex justify-content-end gap-2 mt-4">
        <a href="{{ path('app_department_index') }}" class="btn btn-secondary">Отмена</a>
        <button class="btn btn-primary">{{ button_label|default('Сохранить') }}</button>
    </div>
{{ form_end(form) }}